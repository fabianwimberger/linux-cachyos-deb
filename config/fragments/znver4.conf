# znver4: -march=znver4, as the CachyOS binary repository ships it. Zen 4
# (family 19h) and Zen 5 (family 1Ah). Tuned for AMD, so the package refuses to
# install on anything else even where the ISA would technically run.
-d GENERIC_CPU
-e MZEN4
-d X86_NATIVE_CPU
